import React from 'react';
import ArchitectHero from '../components/hero/ArchitectHero';

/**
 * HeroSection — Architectural Dotted-Globe Hero with Staged Atmospheric Descent & Telemetry
 * Inspired by United Carriers / Meridian study, redesigned in Saravana's architectural terms.
 */
export default function HeroSection({ onNavigate }) {
  return <ArchitectHero onNavigate={onNavigate} />;
}
